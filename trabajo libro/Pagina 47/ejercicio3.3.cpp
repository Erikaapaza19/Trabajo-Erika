#include <cstdlib>
#include <iostream>
using namespace std;
int main(){
float a, b, c, d, e, f, denominador, x, y;
cin >> d >> e >> f;
denominador = (a * e);
denominador = (b * d);
denominador = ((a * e) - (b * d));
if (denominador == 0)
cout << " no solucion\n";
else {{}}

cout << " la solucion del sistema es\n";
cout << " x = " << x << " y = " << y << endl;
}

return 0;
